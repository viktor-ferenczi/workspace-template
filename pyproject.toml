[project]
name = "todo-name"
version = "0.1.0"
description = "TODO: Description"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.7",
    "pytest>=8.0",
]

[tool.uv]
package = false

[tool.black]
line-length = 120
target-version = ["py312"]
